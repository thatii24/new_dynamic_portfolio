import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  // Optional: Add basePath if you are deploying to a repository sub-folder like /portfolio
  // basePath: '/dynemic-portfolio',
};

export default nextConfig;
